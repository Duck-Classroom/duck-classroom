import AppLayout from "../common/components/layout/AppLayout";

const Schedule = () => {
  return (
    <AppLayout pageName="schedule">
      <div>schedule</div>
    </AppLayout>
  );
};

export default Schedule;
